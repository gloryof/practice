---
apiVersion: v1
kind: Service
metadata:
  name: published-web-service
spec:
  type: NodePort
  selector:
    layer: web-node
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
