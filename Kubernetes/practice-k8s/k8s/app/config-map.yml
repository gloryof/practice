apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  application.yaml: |-
    spring:
      redis:
        sentinel:
          master: mymaster
          nodes: redis-sentinel-service:6379